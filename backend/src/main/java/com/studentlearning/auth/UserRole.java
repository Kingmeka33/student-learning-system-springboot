package com.studentlearning.auth;

// Spring Boot equivalent of an enum for role variables.
// Change values here once and the rest of the app can reuse them.
public enum UserRole {
  ADMIN,
  STUDENT
}
